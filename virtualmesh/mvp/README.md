# MVP (Track C) — see ../paper/README.md "MVP" section for full spec.
Assemble ONLY gate-passed pieces into a runnable virtual-mesh demo on 4-8 real frozen models:
frame-align -> coupled DEQ-style settling -> MZ low-rank routing index -> decode; distilled
transitive edges if G3 passes. Show a split-knowledge query the mesh answers that pooling/single-model
does not. Minimal, honest, mechanism-on-real-models — NOT the formal machinery. Gate reds shrink scope.
