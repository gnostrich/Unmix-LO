# core (experiments + extractor)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# only for the real-gradient GATE (Step 1-2): collecting LoRA gradients from a real model
# torch>=2.1
# transformers>=4.40
# peft>=0.10
# datasets>=2.18
